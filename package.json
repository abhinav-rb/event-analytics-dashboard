{
  "name": "event-analytics-dashboard",
  "version": "0.1.0",
  "private": true,
  "description": "A simple TypeScript system that collects events and displays analytics in a dashboard.",
  "workspaces": [
    "server",
    "web"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=server & npm run dev --workspace=web",
    "dev:server": "npm run dev --workspace=server",
    "dev:web": "npm run dev --workspace=web",
    "build": "npm run build --workspace=server && npm run build --workspace=web",
    "capture": "npm run capture --workspace=server"
  }
}
